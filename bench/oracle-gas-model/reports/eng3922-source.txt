# Provenance of the three reports/eng3922-* files — ENG-3922's measurements, as this page reads
# them: the write-side batch rows the batch dial uses for the EAS arm and for the bespoke store's
# batched write, and (ENG-3944) the read-side rows the Read side panel renders.
#
# Every ENG-3922 number on this page traces to a measured row in a named report. Two of the three
# reports are vendored VERBATIM from ENG-3922's merged commit 55058ab0; the arms report is
# REGENERATED on this branch by ENG-3964, from the same harness at the same pinned fork block, to
# add the rows ENG-3944 had to exclude. See the provenance block at the foot of this file for the
# row-by-row accounting.
#
# `file` names the arms report for backward compatibility; `files` lists all three. `commit` is the
# commit the baseline and evidence are byte-identical to, and the commit whose harness produced
# every row in all three.
#
file:   bench-reports/eng3922-arms.txt
files:  bench-reports/eng3922-arms.txt, bench-reports/eng3922-baseline.txt, bench-reports/eng3922-sepolia-evidence.md
armsRegeneratedBy: ENG-3964
armsRowsBefore: 128
armsRowsAfter: 293
armsRowsMoved: 8
armsRowsMovedReason: cassatine W1 on PR #47 -- _easIndexBatch cooled the Indexer alone, leaving EAS warm from the priming multiAttest inside the same test. indexAttestations is necessarily a SEPARATE transaction (a publisher cannot know the uids until the attest transaction returns, ENG-3922's own evidence report says so), so in reality it reads those attestations cold. The 8 indexAttestations rows are +26,000 gas per item as a result. The correction makes arm 1 dearer, not cheaper.
repo:   fabrica-land/fabrica-v3-contracts
pr:     42
branch: sebadas/eng-3922-fact-layer-bench
commit: 55058ab0c9ec6bce5d9ef3779f9dafe22ec10552
status: final
# PROVENANCE IS NOW SPLIT (ENG-3964). Two of the three are still byte-identical to 55058ab0; the
# arms report is regenerated on this branch, because ENG-3964 measured the terms ENG-3944 had to
# exclude and those rows do not exist at 55058ab0.
#
#   eng3922-baseline.txt         md5 3675db2610be82c48a972f6cb60ec3e2   3,133 B  IDENTICAL to 55058ab0
#   eng3922-sepolia-evidence.md  md5 396077400c05554a8e865e50e300a729  18,287 B  IDENTICAL to 55058ab0
#   eng3922-arms.txt             REGENERATED by ENG-3964 from the SAME harness at the SAME pinned
#                                fork block (11,628,000) with the SAME forge (1.7.1, 4072e48).
#                                Was md5 c88f7b824eeb93f00948b2939b677925, 17,695 B at 55058ab0.
#
# The regeneration ADDS rows and MOVES none. Verified row by row against the 55058ab0 copy:
#   128 measured rows before -> 293 after; 165 added, 8 MOVED, 0 removed.
# The 8 are every indexAttestations row, corrected upward by 26,000 gas per item -- see
# armsRowsMovedReason. Nothing else moved: multiAttest, pointBatch, multiRevoke, the ownerless
# store rows and every read row are unchanged from 55058ab0.
# build-model-page.py counts the rows in the committed report and REFUSES if armsRowsAfter
# disagrees, because this number was typed once and went stale within the hour: it was taken after
# the first regeneration and three further benches were added afterwards.
# The baseline was re-run in the same pass and reproduced 19/19 rows identically, so it is kept at
# its committed bytes rather than rewritten with a new wall-clock timing line.
#
# That reproduction also retires an open finding: the page previously said this baseline "does not
# currently reproduce" (contracts#42). Re-run at the same head, forge commit and fork block, it
# reproduces exactly.
#
# The write-side rows the batch dial reads are unchanged from the earlier 516026c revision the
# ENG-3938 lane first vendored; 55058ab0 differs from 516026c only in read-side test output.
